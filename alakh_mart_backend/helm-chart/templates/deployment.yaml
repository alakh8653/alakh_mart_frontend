apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "alakh-mart.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "alakh-mart.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "alakh-mart.name" . }}
    spec:
      containers:
        - name: web
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
